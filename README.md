# Youseisan
ようせいさん discord_bot

## ブランチの構造
```
-main (リリースブランチ)
    |
    |-dev (開発ブランチ)
        |
        |-{username} (個人開発ブランチ)
        |-{username}
```

##個人ブランチの作成
```
git checkout dev
git checkout -b username
```
